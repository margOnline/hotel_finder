%h2
  Hotels.com - <span>wake up happy<span>
%p
  With over <b>140,000 hotels</b> worldwide, <b>priced matched guaranteed</b> and <b>1.2 million</b> reviews, why look elsewhere for your next hotel booking.
.strapline Top Destinations
- @destinations.each do |country, value|
  - value.each do |city, hotels|
    .panel
      .panel-head
        %h4 #{city}, #{country}
        %p Over #{hotels.count} hotels
      .panel-content
        - hotels.each do |hotel|
          .details
            %a{href: hotel.url}
              =hotel.name
            .meta
              %span.rating=hotel.rating
              %span.text From    
              - if hotel.previous_price
                %span.prev-price &pound;#{hotel.previous_price}
              %span.current-price &pound;#{hotel.current_price} 
        %a{href: "#"} See all 898 hotels in London

%a{href: "/"}See all of our destination offers
